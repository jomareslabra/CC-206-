import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/CC-206-/',
  server: {
    port: 3000,
  },
  build: {
    outDir: 'dist',
    emptyOutDir: true, // This ensures dist is cleaned before each build
    sourcemap: false,  // Reduces file clutter
    rollupOptions: {
      output: {
        manualChunks: undefined, // Prevents unnecessary chunk splitting
      }
    }
  }
})